{
  "entities": {
    "User": {
      "title": "User",
      "description": "User profile information",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" }
      },
      "required": ["uid", "email"]
    },
    "Status": {
      "title": "Status",
      "description": "Anonymous journal entries/comments",
      "type": "object",
      "properties": {
        "content": { "type": "string" },
        "authorId": { "type": "string" },
        "anonymousName": { "type": "string" },
        "avatarEmoji": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["content", "authorId", "anonymousName", "createdAt"]
    },
    "JournalEntry": {
      "title": "Journal Entry",
      "description": "Private reflection for the user",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "content": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "timestamp": { "type": "number" }
      },
      "required": ["userId", "content", "createdAt"]
    }
  },
  "firestore": {
    "users": {
      "schema": "User",
      "description": "Collection of user profiles"
    },
    "statuses": {
      "schema": "Status",
      "description": "Collection of anonymous mood statuses"
    },
    "journal_entries": {
      "schema": "JournalEntry",
      "description": "Private personal journal entries"
    }
  }
}
